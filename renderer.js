window.TarsiaRenderer=(()=>{const NS='http://www.w3.org/2000/svg';function el(n,a={}){let e=document.createElementNS(NS,n);Object.entries(a).forEach(([k,v])=>e.setAttribute(k,v));return e}function centroid(t){return{x:t.reduce((s,p)=>s+p.x,0)/3,y:t.reduce((s,p)=>s+p.y,0)/3}}function render(svg,cells,assign,solution,order){svg.innerHTML='';let display=solution?cells:order.map(i=>cells[i]);display.forEach((t,di)=>{let oi=solution?di:order[di],poly=el('polygon',{points:t.map(p=>`${p.x},${p.y}`).join(' '),fill:'white',stroke:'#20262e','stroke-width':'2'});svg.append(poly);t.forEach((p,ei)=>{let txt=assign[`${oi}:${ei}`];if(!txt)return;let q=t[(ei+1)%3],m={x:(p.x+q.x)/2,y:(p.y+q.y)/2},c=centroid(t),x=m.x+(c.x-m.x)*.28,y=m.y+(c.y-m.y)*.28,te=el('text',{x,y,'text-anchor':'middle','dominant-baseline':'middle','font-size':'12','font-family':'Arial'});let val=txt.length>28?txt.slice(0,27)+'…':txt;te.textContent=val;svg.append(te)})})}return{render};})();
